import React from 'react';

export const InvotechLogo = ({ size = 'medium', showText = true, className = '' }) => {
  const dimensions = {
    small: { container: 'w-8 h-8', text: 'text-xs', spacing: 'gap-2' },
    medium: { container: 'w-10 h-10', text: 'text-sm', spacing: 'gap-3' },
    large: { container: 'w-12 h-12', text: 'text-base', spacing: 'gap-4' }
  };

  const { container, text, spacing } = dimensions[size] || dimensions.medium;

  return (
    <div className={`flex items-center ${spacing} ${className}`}>
      {/* Circular Logo */}
      <div className={`${container} relative flex-shrink-0`}>
        <svg
          viewBox="0 0 100 100"
          className="w-full h-full"
          xmlns="http://www.w3.org/2000/svg"
        >
          {/* Background Circle */}
          <circle
            cx="50"
            cy="50"
            r="48"
            fill="white"
            stroke="#5ba4d4"
            strokeWidth="2"
          />
          
          {/* Main Circular Arc */}
          <path
            d="M 15 50 A 35 35 0 1 1 50 15"
            fill="none"
            stroke="#5ba4d4"
            strokeWidth="8"
            strokeLinecap="round"
          />
          
          {/* Inner Arc */}
          <path
            d="M 25 50 A 25 25 0 0 1 50 25"
            fill="none"
            stroke="#5ba4d4"
            strokeWidth="6"
            strokeLinecap="round"
          />
          
          {/* Innovation Peak Element */}
          <path
            d="M 35 65 L 45 45 L 55 55 L 65 35"
            fill="none"
            stroke="#5ba4d4"
            strokeWidth="3"
            strokeLinecap="round"
            strokeLinejoin="round"
          />
        </svg>
      </div>

      {/* Text */}
      {showText && (
        <div className="flex flex-col leading-tight">
          <span className={`font-heading font-bold text-sidebar-primary ${text} tracking-tight`}>
            Invotech
          </span>
          <span className={`font-sans text-sidebar-foreground ${text === 'text-xs' ? 'text-xs' : text === 'text-sm' ? 'text-xs' : 'text-sm'} font-medium tracking-wide`}>
            Peak Innovation
          </span>
        </div>
      )}
    </div>
  );
};

export default InvotechLogo;